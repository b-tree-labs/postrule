% When Should a Rule Learn? — TMLR submission (anonymous / under review).
% Built from ../paper-draft.md via build_tmlr.py. Compile with LuaLaTeX
% (handles the UTF-8 the paper uses: α, →, ×, ≥). See RESUBMISSION.md.

\documentclass[10pt]{article}

% Anonymous submission (default). For the two de-anonymized modes:
%   camera-ready : \usepackage[accepted]{tmlr}
%   preprint/arXiv: \usepackage[preprint]{tmlr}
\usepackage{tmlr}

\input{math_commands.tex}

\usepackage{microtype}  % tighter justification → fewer overfull boxes
\usepackage{hyperref}
\usepackage{url}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{longtable,booktabs,array}
\usepackage{calc}  % length arithmetic in pandoc's longtable column specs
\usepackage{etoolbox}
% Shrink wide multi-column longtables so they stop overrunning the margin.
\AtBeginEnvironment{longtable}{\small}
% Figure captions in the source already carry their own "Figure N." (and the
% prose references those numbers, in a non-insertion order). Only figures use
% \caption here (no \caption tables), so drop the kernel's duplicate
% "Figure N:" auto-label and render just the caption text. (The `caption`
% package conflicts with pandoc longtables, so do it at the kernel level.)
\makeatletter
\long\def\@makecaption#1#2{%
  \vskip\abovecaptionskip
  {\small #2\par}%
  \vskip\belowcaptionskip}
\makeatother

% --- pandoc-fragment support macros (the latex writer assumes these) ---
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\pandocbounded}[1]{#1}
\providecommand{\passthrough}[1]{#1}
% pandoc longtable column widths use \real{0.NN}; its standalone template
% defines this — the fragment writer does not, so provide it.
\providecommand{\real}[1]{#1}

% --- Unicode → LaTeX: the paper's source stays UTF-8 (readable); these map
%     each glyph at compile so the TMLR Computer Modern fonts render them. ---
\usepackage{newunicodechar}
\newunicodechar{→}{\ensuremath{\rightarrow}}
\newunicodechar{×}{\ensuremath{\times}}
\newunicodechar{≈}{\ensuremath{\approx}}
\newunicodechar{≤}{\ensuremath{\le}}
\newunicodechar{≥}{\ensuremath{\ge}}
\newunicodechar{α}{\ensuremath{\alpha}}
\newunicodechar{Δ}{\ensuremath{\Delta}}
\newunicodechar{µ}{\ensuremath{\mu}}
\newunicodechar{✓}{\ensuremath{\checkmark}}
\newunicodechar{§}{\S}
\newunicodechar{³}{\textsuperscript{3}}
\newunicodechar{⁴}{\textsuperscript{4}}
\newunicodechar{⁵}{\textsuperscript{5}}
\newunicodechar{⁶}{\textsuperscript{6}}
\newunicodechar{⁻}{\textsuperscript{\ensuremath{-}}}
\newunicodechar{é}{\'e}
\newunicodechar{š}{\v{s}}
\newunicodechar{č}{\v{c}}
\newunicodechar{ć}{\'c}
\newunicodechar{ė}{\.e}
\newunicodechar{Ž}{\v{Z}}

% --- anonymized system name -------------------------------------------------
% Renders "our framework" for the double-blind submission. For camera-ready /
% preprint, change ONE line to reveal the real name:
%   \renewcommand{\sysname}{Postrule}
\newcommand{\sysname}{our framework}
% Code/package identifier (module paths, filenames). Anonymous now; for
% camera-ready: \renewcommand{\pkgname}{postrule}
\newcommand{\pkgname}{ourlib}

\title{When Should a Rule Learn? Transition Curves for Safe Rule-to-ML Graduation}

% In submission mode the tmlr style auto-anonymizes the byline to
% "Under review as submission to TMLR"; this block is a placeholder and is
% only shown in [accepted]/[preprint] modes (replace then).
\author{\name Anonymous Author \email anon@example.org \\
      \addr Anonymous Institution}

\begin{document}
\maketitle

\begin{abstract}
\input{abstract.tex}
\end{abstract}

\input{body.tex}

\end{document}
